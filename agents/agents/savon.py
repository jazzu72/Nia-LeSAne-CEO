class SavonAgent:
    def optimize(self, plan):
        # Yield optimization stub (integrate real finance bridge later)
        logger.info("Optimizing financial aspects")
        return f"{plan} (with 5% yield increase)"
